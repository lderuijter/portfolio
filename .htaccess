RewriteEngine On

# Skip real files and directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything else to index.php?page=
RewriteRule ^(.*)$ index.php?page=$1 [L,QSA]
